# Next Features

## Chat
- Image uploads + extraction (ActiveStorage/S3, paste + drag/drop, persistent previews).
- Chat actions history (surface created items + quick jump).

## Calendar
- Inline edit in event popover (title/time/location).
- Multi-select day range for bulk actions.

## Trends
- Merchant pin/compare view with multi-series overlay.
- Shareable summary card export.

## Transactions
- Preset filter chips + saved views.
- Inline row actions for split/edit (mobile parity).
